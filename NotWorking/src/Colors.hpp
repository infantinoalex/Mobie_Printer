#ifndef COLORS_HPP
#define COLORS_HPP

/*
 * Simple enumarations used to make it easier to deal with colors
 */
enum Colors
{
    Default = -1,
    Blue = 0,
    Red = 1,
    Yellow = 2,
    Green = 3
};

#endif
